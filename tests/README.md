# WaveScout Tests

This directory is the root for pipeline-side tests and small deterministic fixtures.

Planned layout:

```text
tests/
  fixtures/
  pipeline/
```

The initial test work is tracked in [docs/IMPLEMENTATION-BACKLOG.md](../docs/IMPLEMENTATION-BACKLOG.md) under `WS-01`.
